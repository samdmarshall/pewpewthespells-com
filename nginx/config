worker_processes auto;

http {

  server {
    listen 80;
    listen 443 ssl;
    server_name www.pewpewthespells.com;
    
    ssl on;
    ssl_certificate     www.pewpewthespells.com.crt;
    ssl_certificate_key www.pewpewthespells.com.key;
    
    return 301 $scheme://domain.com$request_uri;
  }

  server {
    listen 80;
    listen 433 ssl;
    server_name pewpewthespells.com;
    
    ssl on;
    ssl_certificate     pewpewthespells.com.crt;
    ssl_certificate_key pewpewthespells.com.key;

    location / {
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $host;
      # Jester listens on port 5000 by default
      proxy_pass http://127.0.0.1:5000;
    }
  }
}

events { }
